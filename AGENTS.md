# Piko 项目协作规则

## P2P 传输硬边界

- 本项目的 P2P 文件传输只采用端到端直连能力。
- 本项目不采用 TURN、relay、中继、中转、转发服务器或任何等价的中继式传输方案。
- 该约束是长期产品决策，不作为临时限制处理；未来排查、优化或重构 P2P 链路时，必须默认遵守。
- 不得通过新增 TURN 配置、relay 候选、云端转发、服务端代传、对象存储中转、临时上传下载或类似方式解决 P2P 失败。
- 如果直连失败，允许增加诊断日志、改善 ICE/STUN 采集、调整直连等待策略、优化信令时序或提示真实失败原因，但不得把中继/中转方案包装成修复。
- 如发现某个需求无法在纯直连边界内完成，必须明确说明该需求与本项目传输边界冲突，并停止实现中继/中转相关改动。
